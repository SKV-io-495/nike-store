# A descriptive name for your workflow
name: Build and Push to AWS ECR

# This tells GitHub to run the workflow automatically
# whenever you push code to the "main" branch.
on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: Build and Push Docker Image
    # Use a clean virtual computer provided by GitHub to run the job
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out your repository's code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Configure the AWS credentials using the secrets you created
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # Step 3: Log in to your Amazon ECR "warehouse"
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # Step 4: Build the Docker image and push it to ECR
      - name: Build, tag, and push image to Amazon ECR
        env:
          # Use the repository name and account ID secrets
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ secrets.AWS_ECR_REPOSITORY_NAME }}
          # Use the unique commit ID as the image tag for versioning
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # This is the same build command we figured out, but now it's automated!
          # We use a placeholder for the DATABASE_URL because the build needs it,
          # but we don't need a real one for the CI test.
          docker build --build-arg DATABASE_URL="${{ secrets.DATABASE_URL }}" -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      # FINAL STEP (DISABLED FOR NOW)
      # We will uncomment the following step ONLY after your AWS account
      # is active and we have manually created the App Runner service.
      # - name: Deploy to AWS App Runner
      #   run: |
      #     aws apprunner start-deployment --service-arn ${{ secrets.AWS_APP_RUNNER_SERVICE_ARN }}
